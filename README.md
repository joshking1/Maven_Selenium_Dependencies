# Maven_Selenium_Dependencies
These are the notes for the maven and selenium dependencies. We also have project object management.xml (POM.xml) which contains the vital information regarding the project. 
